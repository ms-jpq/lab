---
services:
  stirling-pdf:
    image: ${REGISTRY_MIRROR:-docker.io}/ubuntu/squid:latest
    labels:
      kompose.controller.type: statefulset
    ports:
      - 3128
    volumes:
      - cache:/var/spool/squid
      - logs:/var/log/squid

volumes:
  cache:
  logs:
