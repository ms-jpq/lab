---
services:
  mosquitto:
    image: ${REGISTRY_MIRROR:-docker.io}/library/eclipse-mosquitto:latest
    ports:
      - 1883
    volumes:
      - data:/mosquitto/data
      - log:/mosquitto/log

volumes:
  data:
  log:
