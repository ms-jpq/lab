---
services:
  smart-ipv6-rotator:
    image: quay.io/invidious/smart-ipv6-rotator:latest
    labels:
      kompose.cronjob.schedule: "0 0,8,16 * * *"
      jq.mixin: |-
        {"hostNetwork": true}
    restart: no
    command:
      - --help
    network_mode: host
