---
services:
  redis-commander:
    image: ghcr.io/joeferner/redis-commander:latest
    labels:
      kompose.service.expose: redis.${DOMAIN}
    environment:
      REDIS_HOSTS: lo:redis:6379:0:${COMPOSE_PROJECT_NAME}
    ports:
      - 8081

  redis:
    image: docker.io/library/redis:latest
    labels:
      kompose.service.type: nodeport
      kompose.service.nodeport.port: m4_esyscmd([printf -- %s '30000 + 6379 % 2767' | bc])
    healthcheck:
      test:
        - CMD
        - redis-cli
        - incr
        - ping
    command:
      - redis-server
      # - --requirepass
      # - ${COMPOSE_PROJECT_NAME}
    ports:
      - 6379
    volumes:
      - data:/data

volumes:
  data:
