---
services:
  anubis:
    image: ghcr.io/techarohq/anubis:latest
    labels:
      kompose.service.expose: twitter.${DOMAIN}
    environment:
      COOKIE_DOMAIN: ${DOMAIN}
      ED25519_PRIVATE_KEY_HEX: m4_esyscmd([b3sum <<< 'DOMAIN' | cut -d ' ' -f 1])
      SERVE_ROBOTS_TXT: true
      TARGET: http://nitter:8080
    ports:
      - 8923

  nitter:
    image: docker.io/zedeus/nitter:latest
    # healthcheck:
    #   test:
    #     - CMD
    #     - wget
    #     - --quiet
    #     - -O/dev/null
    #     - --timeout
    #     - "1"
    #     - --
    #     - 127.0.0.1:8080
    ports:
      - 8080
    volumes:
      - ./nitter.toml:/src/nitter.conf:ro
      - ./sessions.jsonl:/src/sessions.jsonl:ro

  redis:
    image: docker.io/library/redis:latest
    healthcheck:
      test:
        - CMD
        - redis-cli
        - incr
        - ping
    ports:
      - 6379
    volumes:
      - data:/data

volumes:
  data:
