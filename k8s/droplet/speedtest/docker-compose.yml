---
services:
  speedtest:
    image: ghcr.io/librespeed/speedtest:latest
    labels:
      kompose.service.expose: speedtest.${DOMAIN}
    healthcheck:
      test:
        - CMD
        - curl
        - --fail
        - --location
        - --no-progress-meter
        - --output
        - /dev/null
        - --max-time
        - "1"
        - --
        - localhost:80
    ports:
      - 80
    volumes:
      - data:/database

volumes:
  data:
