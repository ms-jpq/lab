---
services:
  ollama:
    image: docker.io/ollama/ollama:latest
    labels:
      kompose.controller.type: statefulset
      kompose.service.expose: ollama
      kompose.volume.type: hostPath
    runtime: nvidia
    ports:
      - 11434
    volumes:
      - /var/lib/local/ollama:/root/.ollama
