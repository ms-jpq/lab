[project]
dependencies = ["elasticsearch", "aws-lambda-powertools", "miniflux"]
name         = "lab"
version      = "0"

[project.optional-dependencies]
dev = ["mypy", "types-PyYAML", "black", "isort"]
