---
scrape_configs:
  # m5_for([MACH], [
  - job_name: MACH
    basic_auth:
      username: "8080"
      password: m5_esyscmd(printf -- %s 'SALT=ENV_MAIL'$(cut -d . -f 1 <<< 'MACH') | b3sum | cut -d ' ' -f 1)
    static_configs:
      - targets:
          - MACH:8080
  # ], m5_assert([ENV_PROMETHEUS_HOSTS]))
