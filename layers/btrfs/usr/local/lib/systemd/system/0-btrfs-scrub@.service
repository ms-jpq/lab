[Unit]
Description       = Btrfs Scrub Service
RequiresMountsFor = %f

[Service]
Type              = oneshot
ExecStart         = btrfs scrub start -B -- %f
ExecStop          = btrfs scrub cancel -- %f
