[Service]
ProtectSystem   = strict
ProtectHome     = yes
PrivateTmp      = yes

# TODO: ubuntu 26 -> new procfs library needed for kernel changes
EnvironmentFile =
Environment     = ARGS=--no-collector.xfs
