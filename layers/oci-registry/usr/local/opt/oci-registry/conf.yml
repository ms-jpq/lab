---
version: 0.1
storage:
  filesystem:
    rootdirectory: ${CACHE_DIRECTORY}
proxy:
  remoteurl: ${PROXY_REMOTEURL}
  ttl: 72h
http:
  net: unix
  relativeurls: true
  addr: ${RUNTIME_DIRECTORY}/${RT_ID}.sock
  prefix: /${RT_ID}/
