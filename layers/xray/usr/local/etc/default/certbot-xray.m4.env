# m4_include([layers/xray/usr/local/opt/xray/vars.m4.yml])

# m5_for([PROTOCOL], [
# m5_for([TRANSPORT], [
# m4_pushdef([TRANS], [m4_regexp(TRANSPORT, [\(^[^@]+\)], [\1])])
# m4_pushdef([XPORT], [m4_regexp(TRANSPORT, [\([^@]+$\)], [\1])])
# m4_pushdef([SALTED], m4_substr(m5_esyscmd(printf -- %s 'PROTOCOL-TRANS-ENV_XRAY_SEED' | b3sum --no-names --length 8), 0, 16))

SALTED.x.m5_assert([ENV_XRAY_DOMAIN])

# ], TRANSPORTS)
# ], PROTOCOLS)
