bind-dynamic
bootp-dynamic
cache-size=0
clear-on-reload
conf-dir=${RUN}/conf.d,*.conf
conf-dir=/usr/local/opt/dnsmasq/conf.d,*.conf
dhcp-authoritative
dhcp-client-update
dhcp-fqdn
dhcp-generate-names=tag:${IFACE}
dhcp-hostsdir=${RUN}/dhcp.hosts.d
dhcp-leasefile=${RUN}/leases
dhcp-option=option6:dns-server,[fd00::]
dhcp-option=option6:domain-search,${DOMAIN}
dhcp-option=option6:ntp-server,[fd00::]
dhcp-option=option6:sntp-server,[fd00::]
dhcp-option=option:dns-server,0.0.0.0
dhcp-option=option:domain-name,${DOMAIN}
dhcp-option=option:domain-search,${DOMAIN}
dhcp-option=option:ntp-server,0.0.0.0
dhcp-option=option:router,0.0.0.0
dhcp-option=option:server-ip-address,0.0.0.0
dhcp-optsdir=${RUN}/dhcp.opts.d
dhcp-range=${IPV4_MINADDR},${IPV4_MAXADDR},${TTL}
dhcp-range=::,constructor:${IFACE},ra-stateless,ra-names,64,${TTL}
dhcp-range=::2,::ffff:ffff:ffff:ffff,constructor:${IFACE},slaac,64,${TTL}
dhcp-rapid-commit
dhcp-script=/usr/local/opt/dnsmasq/libexec/dhcp-script.sh
dns-forward-max=999999
domain=${DOMAIN}
enable-ra
hostsdir=${RUN}/hosts.d
interface-name=_gateway.${DOMAIN},${IFACE}
interface=${IFACE}
local-service
local=/${DOMAIN}/
localmx
log-facility=-
neg-ttl=0
no-hosts
no-negcache
no-poll
no-resolv
pid-file=${PIDFILE}
proxy-dnssec
quiet-dhcp
quiet-dhcp6
quiet-ra
ra-param=*,6,${TTL}
script-on-renewal
selfmx
server=127.0.0.53
strict-order
