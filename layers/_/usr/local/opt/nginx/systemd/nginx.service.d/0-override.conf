[Unit]
Upholds           = 0-nginx-sidecar.service
Requires          = 0-self-signed@%p.service
After             = 0-self-signed@%p.service

[Service]
ProtectSystem     = strict
PrivateTmp        = yes
BindReadOnlyPaths = /usr/local/opt/nginx/conf/main.nginx:%E/nginx/nginx.conf
ReadWritePaths    = %t %T %V
RuntimeDirectory  = local/%p local/%p/conf.d local/%p/server.d local/%p/http.d local/%p/stream.d local/%p/www
