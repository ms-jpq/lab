server {
  listen               8080;
  listen               [::]:8080;
  auth_basic           "8080";
  auth_basic_user_file /var/lib/local/nginx/8080.htpasswd;

  location / {
    proxy_pass http://unix:/run/local/nginx/cgi.sock:/;
  }
}