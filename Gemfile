# frozen_string_literal: true

source("https://rubygems.org")

group(:development) do
  %w[rubocop rubocop-performance rubocop-thread_safety syntax_tree].each do
    gem(_1, require: false)
  end
end
