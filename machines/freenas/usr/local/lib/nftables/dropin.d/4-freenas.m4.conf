# NL

define TRANSMISSION = 9091;
define PLEX = 32400;

add element inet user internal_ifs   { m5_assert([ENV_MACVLAN_IF]) }

add element inet user internal_ports { tcp . $TRANSMISSION }
add element inet user external_ports { tcp . http, tcp . https, udp . https, tcp . $PLEX }
