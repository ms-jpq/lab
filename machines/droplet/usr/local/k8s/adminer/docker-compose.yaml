---
services:
  adminer:
    image: docker.io/library/adminer:latest
    userns_mode: auto
    restart: always
    labels:
      - kompose.controller.type=statefulset
      - kompose.service.expose=postgres.${DOMAIN}
    ports:
      - 8080
