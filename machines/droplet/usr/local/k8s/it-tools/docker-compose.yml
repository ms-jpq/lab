---
services:
  it-tools:
    image: ghcr.io/corentinth/it-tools:latest
    healthcheck:
      test:
        - CMD
        - curl
        - --fail
        - --no-progress-meter
        - --max-time
        - "1"
        - --
        - 127.0.0.1:80
    labels:
      kompose.service.expose: tools.${DOMAIN}
    ports:
      - 80
