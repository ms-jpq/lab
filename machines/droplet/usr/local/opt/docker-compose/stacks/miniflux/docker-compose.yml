---
services:
  srv:
    image: ghcr.io/miniflux/miniflux:latest
    userns_mode: auto
    restart: on-failure
    depends_on:
      pg:
        condition: service_healthy
    healthcheck:
      test:
        - CMD
        - wget
        - --quiet
        - --output-document
        - /dev/null
        - --timeout
        - "1"
        - --
        - localhost:8080
    labels:
      - traefik.enable=true
      - traefik.http.services.${COMPOSE_PROJECT_NAME}.loadbalancer.server.port=8080
      - traefik.http.routers.${COMPOSE_PROJECT_NAME}.rule=HostRegexp(`rss.{.+}.{.+$$}`)
    environment:
      ADMIN_PASSWORD:
      ADMIN_USERNAME:
      CREATE_ADMIN: 1
      DATABASE_URL: postgres://postgres:postgres@pg/postgres?sslmode=disable
      INVIDIOUS_INSTANCE:
      PROXY_IMAGES: all
      PROXY_MEDIA_TYPES: image,audio,video
      PROXY_OPTION: all
      RUN_MIGRATIONS: 1
    networks:
      - default
      - traefik

  pg:
    image: docker.io/library/postgres:latest
    userns_mode: auto
    healthcheck:
      test:
        - CMD
        - pg_isready
        - --username
        - postgres
        - --dbname
        - postgres
    environment:
      POSTGRES_DB: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
    volumes:
      - /var/lib/local/${COMPOSE_PROJECT_NAME}:/var/lib/postgresql/data

networks:
  traefik:
    external: true
    name: traefik
