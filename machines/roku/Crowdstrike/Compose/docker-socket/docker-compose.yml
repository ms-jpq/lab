---
services:
  docker-socket-proxy:
    restart: unless-stopped
    image: docker.io/library/nginx:latest
    ports:
      - 2375:2375
    volumes:
      - ./socket-proxy.nginx:/etc/nginx/nginx.conf:ro
      - /var/run/docker.sock:/var/run/docker.sock
