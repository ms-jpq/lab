CPU=16,cores=8,threads=2
MEMORY=16G
# VNC_SOCK=
# m4_define([VFIO], [m5_for([ID], [--vfio ID ], m5_assert([ENV_VFIO_PCI_DEVICE_IDS]))])
# m4_define([ISCSI], [--drive iscsi://freenas.enp2s0/iqn.1949-08.arpa.home.iscsi:freenas%40steam/0])
# m4_define([IOPS], [100])
# m4_define([IOPS_LEN], [60])
# m4_define([IOPS_MUX], [150])
# m4_define([SLOW], [throttling.iops-total=IOPS,throttling.iops-total-max-length=IOPS_LEN,throttling.iops-total-max=m4_eval(IOPS_LEN * IOPS * IOPS_MUX / 100)])
# [SLOW]=SLOW
ARGV= VFIO ISCSI,SLOW
